# Scala 3 on JavaScript

This is a fork of the [Scala 3](https://github.com/scala/scala3) that runs on JavaScript.

## Standalone Binaries

With `bun`, we package the Scala 3 compiler into standalone binaries. Check the release page.
