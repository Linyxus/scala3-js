# An `import` in one line is in scope for later lines.
>>> {"op":"eval","code":"val r = evalLoop[Int] { (ctx, s) => s.eval(\"import scala.collection.mutable.ListBuffer\"); val v = s.eval[Int](\"val b = ListBuffer(1, 2, 3); b.size\"); s.complete(v) }"}
~ "ok":true.*val r: Int = 3
