# Eval'd lines see prior REPL top-level definitions (the outer session), the
# same way one-shot eval does.
>>> {"op":"eval","code":"val n = 100"}
~ "ok":true

>>> {"op":"eval","code":"val r = simpleRepl[Int] { s => val v = s.eval[Int](\"n + 1\"); s.complete(v) }"}
~ "ok":true.*val r: Int = 101
