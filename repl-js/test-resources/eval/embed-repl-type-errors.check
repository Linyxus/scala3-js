# complete's argument must conform to R.
>>> {"op":"eval","code":"simpleRepl[Int] { s => s.complete(\"not an int\") }"}
~ "ok":false

# The body must have type Nothing — it cannot fall through without completing.
>>> {"op":"eval","code":"simpleRepl[Int] { s => val x = 1 }"}
~ "ok":false

# An empty line is a compile error inside the simpleRepl session.
>>> {"op":"eval","code":"val r = simpleRepl[Int] { s => s.eval[Int](\"\"); s.complete(0) }"}
~ "ok":false
