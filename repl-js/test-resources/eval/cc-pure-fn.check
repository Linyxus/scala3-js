# cc 1: the pure-function arrow `->` parses inside the body only if cc is
# forwarded into the inner compile.
>>> {"op":"eval","code":"import language.experimental.captureChecking"}
~ "ok":true

>>> {"op":"eval","code":"val r: Int = eval(\"val f: Int -> Int = (n: Int) => n + 1; f(41)\")"}
~ "ok":true.*val r: Int = 42
