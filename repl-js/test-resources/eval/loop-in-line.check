# An evalLoop can appear inside an s.eval'd line (nested loop driven by eval'd
# code rather than the outer body).
>>> {"op":"eval","code":"val r = evalLoop[Int] { (ctx, s) => val v = s.eval[Int](\"evalLoop[Int] { (c2, s2) => s2.complete(8) }\"); s.complete(v) }"}
~ "ok":true.*val r: Int = 8
